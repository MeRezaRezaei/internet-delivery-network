global
    # --------------------------------------------------------------------------
    # GLOBAL SETTINGS
    # --------------------------------------------------------------------------
    log stdout format raw local0
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners
    stats timeout 30s
    user haproxy
    group haproxy
    daemon
    
    # SSL/TLS Configuration
    tune.ssl.default-dh-param 2048
    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private
    ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
    ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
    ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
    # --------------------------------------------------------------------------
    # DEFAULT SETTINGS
    # --------------------------------------------------------------------------
    option httplog
    log-format "%ci:%cp [%tr] %ft %b/%s %TR/%Tw/%Tc/%Tr/%Ta %ST %B %CC %CS %tsc %ac/%fc/%bc/%sc/%rc %sq/%bq %hr %hs %{+Q}r"
    log global
    mode http
    option dontlognull
    timeout connect 5000
    timeout client  50000
    timeout server  50000

frontend main_http
    # --------------------------------------------------------------------------
    # HTTP FRONTEND (Port 80)
    # --------------------------------------------------------------------------
    bind :::80 v4v6
    mode http

    # Identify tunnel paths to exclude them from the HTTPS redirect
    acl is_tunnel path -i -m beg /21-08-07-05 /24-01-07-06 /22-04-07-06 /23-01-07-05 /21-08-07-06 /24-10-07-06 /100-10-01-05
    
    # Redirect standard traffic to HTTPS, bypassing the redirect for tunnels
    http-request redirect scheme https if !{ ssl_fc } !is_tunnel

    # Route active HTTP tunnel paths to their backends
    # Note: XTLS must be evaluated before VLESS to ensure proper path matching
    use_backend bk_srv08_xtls if { path_beg /21-08-07-05/xtls }
    use_backend bk_srv01_xtls if { path_beg /24-01-07-06/xtls }
    use_backend bk_srv10_xtls if { path_beg /24-10-07-06/xtls }

    use_backend bk_srv08_vless if { path_beg /21-08-07-05 }
    use_backend bk_srv04_vless if { path_beg /22-04-07-06 }
    use_backend bk_srv09_vless if { path_beg /23-01-07-05 }
    use_backend bk_srv01_vless if { path_beg /24-01-07-06 }
    use_backend bk_srv06_vless if { path_beg /21-08-07-06 }
    use_backend bk_srv10_vless if { path_beg /24-10-07-06 }
    use_backend bk_srv01_100_tunnels if { path_beg /100-10-01-05 }

    default_backend bk_forward_http

frontend main_https
    # --------------------------------------------------------------------------
    # HTTPS FRONTEND (Port 443)
    # --------------------------------------------------------------------------
    bind :::443 v4v6 ssl crt /etc/ssl/private/selfsigned.pem crt /etc/ssl/private/ehraz.pem alpn h2,http/1.1
    mode http
    option forwardfor

    # Standard Proxy Headers required by Marzban and other panels
    http-request set-header X-Forwarded-Proto https
    http-request set-header X-Forwarded-Host %[hdr(host)]
    http-request set-header X-Forwarded-Port 443

    # Direct Route
    use_backend bk_mmd_pg_us if { path_beg /mmd-pg-us }

    # --- 1. XRAY TUNNELS ROUTING ---
    # XTLS paths evaluated first
    use_backend bk_srv08_xtls if { path_beg /21-08-07-05/xtls }
    use_backend bk_srv01_xtls if { path_beg /24-01-07-06/xtls }
    use_backend bk_srv10_xtls if { path_beg /24-10-07-06/xtls }

    # VLESS paths evaluated second
    use_backend bk_srv08_vless if { path_beg /21-08-07-05 }
    use_backend bk_srv04_vless if { path_beg /22-04-07-06 }
    use_backend bk_srv09_vless if { path_beg /23-01-07-05 }
    use_backend bk_srv01_vless if { path_beg /24-01-07-06 }
    use_backend bk_srv06_vless if { path_beg /21-08-07-06 }
    use_backend bk_srv10_vless if { path_beg /24-10-07-06 }
    use_backend bk_srv01_100_tunnels if { path_beg /100-10-01-05 }

    use_backend bk_doh if { path_beg /dns-query }

    # --- 2. SUBDOMAIN ROUTING (MARZBAN & SERVICES) ---
    # tech.new-state.ir -> Technitium DNS Management
    acl is_tech hdr(host) -i tech.new-state.ir
    use_backend bk_technitium if is_tech

    # dash.new-state.ir -> Migrated Panel (400 Users)
    acl is_dash_migrated hdr(host) -i dash.new-state.ir
    use_backend bk_marzban_pubg if is_dash_migrated

    acl is_sub_migrated hdr(host) -i sub.new-state.ir
    use_backend bk_marzban_pubg if is_sub_migrated

    # panel.new-state.ir -> Old Panel (5 Users)
    acl is_panel_old hdr(host) -i panel.new-state.ir
    use_backend bk_marzban_main if is_panel_old

    # Fallback/Legacy Redirects for Marzban roots
    acl is_marzban_root path_beg -i /dashboard /sub /api /statics
    use_backend bk_marzban_pubg if is_marzban_root

    default_backend bk_forward


# ==============================================================================
# BACKENDS DEFINITION
# ==============================================================================

# --- Panels & Infrastructure ---
backend bk_technitium
    mode http
    server technitium_local 127.0.0.1:53443 ssl verify none

backend bk_doh
    mode http
    server technitium_local 127.0.0.1:1443 ssl verify none

backend bk_mmd_pg_us
    mode http
    server srv_mmd_pg_us 127.0.0.1:9443 proto h2

backend bk_marzban_main
    mode http
    server marzban_main 127.0.0.1:2020

backend bk_marzban_pubg
    mode http
    server marzban_pubg 127.0.0.1:8002

backend local_nginx_http
    mode http
    server nginx_local 127.0.0.1:8080

# --- Default Forwarding ---
backend bk_forward
    mode http
    server doh_node 10.255.1.10:443 ssl verify none

backend bk_forward_http
    mode http
    server doh_node_http 10.255.1.10:80

# --- Xray XTLS Tunnels ---
backend bk_srv08_xtls
    mode http
    server xray_srv08_xtls 127.0.0.1:5011

backend bk_srv01_xtls
    mode http
    server xray_srv01_xtls 127.0.0.1:5012

backend bk_srv10_xtls
    mode http
    server xray_srv10_xtls 127.0.0.1:5013

# --- Xray VLESS Tunnels ---
backend bk_srv08_vless
    mode http
    server xray_srv08 127.0.0.1:21075

backend bk_srv04_vless
    mode http
    server xray_srv04 127.0.0.1:22076

backend bk_srv09_vless
    mode http
    server xray_srv09 127.0.0.1:23075

backend bk_srv01_vless
    mode http
    server xray_srv01 127.0.0.1:24076

backend bk_srv06_vless
    mode http
    server xray_srv06 127.0.0.1:21076

backend bk_srv10_vless
    mode http
    server xray_srv10 127.0.0.1:65443

# --- 100-Tunnel Speed Aggregator ---
backend bk_srv01_100_tunnels
    mode http
    server xray_srv01_100 10.255.1.1:15100
